const start = 0.2
const increment = 0.1
const max = 0.8

const startMultiplied = start * 100
const incrementMultiplied = increment * 100
const maxMultiplied = max * 100

console.log(startMultiplied, incrementMultiplied, maxMultiplied)
